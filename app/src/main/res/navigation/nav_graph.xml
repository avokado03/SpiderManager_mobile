<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/SpidersFragment">

    <fragment
        android:id="@+id/SpidersFragment"
        android:name="com.app.spidermanager.SpiderFragment"
        android:label="@string/app_name"
        tools:layout="@layout/spiders_fragment">

        <action
            android:id="@+id/action_SpidersFragment_to_SecondFragment"
            app:destination="@id/SecondFragment" />
    </fragment>
    <fragment
        android:id="@+id/SecondFragment"
        android:name="com.app.spidermanager.SecondFragment"
        android:label="@string/app_name"
        tools:layout="@layout/fragment_second">


        <action
            android:id="@+id/action_SecondFragment_to_SpidersFragment"
            app:destination="@id/SpidersFragment" />
    </fragment>
</navigation>